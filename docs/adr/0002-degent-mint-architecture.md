# ADR-0002: degent.club automated mint architecture

This decision belongs to the degent product and lives in **DegentClub/degent** at `docs/adr/0002-degent-mint-architecture.md` (see ADR-0004: a record lives with the repository that owns the component it decides). The number stays reserved here so cross-references in `platform/` remain stable.
