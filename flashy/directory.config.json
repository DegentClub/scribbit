{
  "//": "Options for `mesh emit-directory` (@bsh/mesh directoryFromCharter). Everything the org and its roles imply is read from charter.json; only what this repository is additionally the authority for is here. `asserted` is pinned so the emitted fragment is reproducible in CI.",
  "asserted": "2026-09-24",
  "expires": "2027-09-24",
  "assertedBy": "person/bsh-accountable",
  "platform": "bitcoin",
  "vertical": "scribbit",
  "properties": [
    {
      "domain": "scribb.it",
      "name": "scribb.it"
    }
  ],
  "emitters": [
    {
      "id": "agent/scribbit-records",
      "name": "Record emitter",
      "formats": [
        "shipped/1",
        "devlog/1",
        "checkpoint/1"
      ],
      "description": "The `mesh emit` run that derives this repository's record from git, seals each shipped/1 entry as agent/scribbit-records, writes the devlog beside it and the checkpoint/1 head over what is served."
    }
  ],
  "externalsOut": "directory.externals.json"
}
